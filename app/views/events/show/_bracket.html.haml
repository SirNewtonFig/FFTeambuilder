#events.divide-y.divide-gray-500
  - teams.each do |team|
    .flex.justify-between{ id: dom_id(team, 'bracket') }
      = render 'events/submissions/submission', team:

      %a.group.block.py-4.text-orange-400.hover:text-orange-300{ href: cut_event_submission_path(event_id: event.id, id: team.id), data: { turbo_method: :patch } }
        Cut

  - if teams.blank?
    .py-4.text-gray-600.italic There are no confirmed teams for this event yet
